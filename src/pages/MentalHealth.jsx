import MentalHealthExperience from "../features/mental-health/MentalHealthExperience.jsx";

/** Mental Health — calm emotional companion (React), not the legacy psychology iframe. */
export default function MentalHealth() {
  return <MentalHealthExperience />;
}
